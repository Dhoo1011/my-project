# Phantom RP - نظام إدارة الشرطة

تطبيق ويب متقدم لإدارة أنظمة الشرطة والبيانات المتعلقة بالجرائم والمشتبه بهم.

## المميزات ✨

- 📊 لوحة تحكم متقدمة
- 👮 إدارة الأقسام والموظفين
- 🚨 نظام الإنذارات والبلاغات
- 📋 إدارة السجلات الجنائية
- 🔐 نظام أمان قوي مع جلسات آمنة
- 📱 واجهة مستجيبة (Responsive Design)
- 🌍 دعم اللغة العربية

## المتطلبات

- Node.js 16+
- PostgreSQL 12+
- npm أو yarn

## التثبيت المحلي

```bash
# استنساخ المستودع
git clone https://github.com/yourusername/my-project.git
cd my-project

# تثبيت التبعيات
npm install

# إنشاء ملف البيئة
cp .env.example .env

# تعديل DATABASE_URL في .env
# DATABASE_URL=postgresql://user:password@localhost:5432/phantom_rp

# إنشاء جداول قاعدة البيانات
npm run db:push

# تشغيل خادم التطوير
npm run dev
```

سيكون التطبيق متاحاً على `http://localhost:5000`

## النشر على Render

### الطريقة الأولى: النشر المباشر

1. اذهب إلى [render.com](https://render.com)
2. سجل حساباً جديداً
3. اختر "New +" ثم "Blueprint"
4. ربط مستودعك على GitHub
5. Render سيكتشف `render.yaml` تلقائياً ويقوم بالنشر

### الطريقة الثانية: النشر اليدوي

1. أنشئ خدمة Web جديدة على Render
2. أضف متغيرات البيئة:
   - `NODE_ENV=production`
   - `DATABASE_URL=[سيتم توليده تلقائياً]`
   - `SESSION_SECRET=[توليد عشوائي]`
3. أضف قاعدة بيانات PostgreSQL
4. قم بـ Deploy

### الرابط المتوقع للتطبيق
```
https://your-project.onrender.com
```

## النشر على منصات أخرى

### Railway.app
```bash
npm install -g railway
railway login
railway init
railway up
```

### Heroku (يتطلب بطاقة ائتمان)
```bash
npm install -g heroku
heroku create your-app-name
heroku addons:create heroku-postgresql:free
git push heroku main
```

## البناء والإنتاج

```bash
# بناء المشروع
npm run build

# التحقق من TypeScript
npm run check

# تشغيل النسخة الإنتاجية محلياً
npm start
```

## هيكل المشروع

```
my-project/
├── client/                 # واجهة المستخدم (React + Vite)
│   ├── src/
│   │   ├── components/    # مكونات React
│   │   ├── pages/         # الصفحات
│   │   ├── hooks/         # Hooks مخصصة
│   │   └── lib/           # مكتبات وأدوات
│   └── index.html
├── server/                # الخادم (Express.js)
│   ├── routes.ts          # المسارات
│   ├── db.ts              # إعدادات قاعدة البيانات
│   └── index.ts           # نقطة الدخول
├── shared/                # الكود المشترك
│   ├── schema.ts          # تعريفات Drizzle ORM
│   └── routes.ts
└── script/                # سكريبتات البناء
```

## المتغيرات البيئية المهمة

| المتغير | الوصف | المثال |
|---------|-------|--------|
| DATABASE_URL | رابط قاعدة البيانات | postgresql://user:pass@host/db |
| NODE_ENV | بيئة التشغيل | production / development |
| SESSION_SECRET | مفتاح الجلسات | secret-key-here |
| PORT | منفذ الخادم | 5000 |

## الدعم التقني

للإبلاغ عن المشاكل أو الاقتراحات، يرجى فتح issue في المستودع.

## الترخيص

MIT License - انظر ملف LICENSE للتفاصيل
